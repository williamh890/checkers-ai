[general]
run = [1 or 0, 1 runs, 0 will cause director to stop at end of next tournament]

[paths]
network_path = [Doesn't matter, I need to take this out,  set it to whatever]
checkers_path = [[path to your checkers_ai repo]/checkers-ai]

[functions]
checkers_game=checkers.out
network_manager=manager.out
network_count = [number of networks in your directory]

[processes]
max_processes=3
